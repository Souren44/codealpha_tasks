# codealpha_tasks
Objective of this project is to detect human emotions from speech ausio using Deep Learning model( Tensorflow), LSTM architecture and Speech Signal Preprocessing techniques(MFCC(Mel Frequency Cepstral Coefficients)). After training model i built an interactive web application where users can upload a WAV audio file and the model predicts emotion.
